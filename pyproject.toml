[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "saag-msd"
version = "0.1.0"
description = "SaaG-MSD: Model Setup Data Generation (CSU MSD)"
requires-python = ">=3.11"
dependencies = [
    "saag-contracts",
    "iPOPO>=3.2,<4",
    "fastapi",
    "sqlalchemy",
    "psycopg[binary]",
    "pymysql",
    "pyyaml",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "httpx2",
    "ruff",
]

[project.entry-points."saag.bundles"]
msd = "saag_msd.bundle"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
saag_msd = [
    "adapters/*.json",
    "testing/data/**/*",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "integration: exercises a real external system; skipped unless one is configured",
]

[tool.uv.sources]
saag-contracts = { workspace = true }
