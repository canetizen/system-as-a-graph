[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "saag-vae-design-verifier"
version = "0.1.0"
description = "SaaG-VAE: Design Verifier (CSU VAE-02)"
requires-python = ">=3.11"
license = "Apache-2.0"
dependencies = [
    "saag-contracts>=0.1,<1",
    "iPOPO>=3.2,<4",
    "fastapi",
]

[project.urls]
Repository = "https://github.com/onuralpyigit/system-as-a-graph"


[project.entry-points."saag.bundles"]
vae-02 = "saag_vae_design_verifier.bundle"

[dependency-groups]
dev = [
    "pytest",
    "httpx2",
    "ruff",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
