[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "saag-vae-design-analyzer"
version = "0.1.0"
description = "SaaG-VAE: Design Analyzer (CSU VAE-03)"
requires-python = ">=3.11"
dependencies = [
    "saag-contracts",
    "iPOPO>=3.2,<4",
    "fastapi",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "httpx2",
    "ruff",
]

[project.entry-points."saag.bundles"]
vae-03 = "saag_vae_design_analyzer.bundle"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.uv.sources]
saag-contracts = { workspace = true }
